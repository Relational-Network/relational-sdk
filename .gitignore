# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright (C) 2026 Relational Network

/target
drt-examples/mean/target

# Ignore Gramine SGX artifacts
relational-sdk.sig
relational-sdk.manifest
relational-sdk.manifest.sgx

# Attestation client build artifacts
attestation-client/attest
attestation-client/ssl/ca.crt
attestation-client/ssl/ca.key

# Encrypted data directory (Gramine sealed)
/data/

# Transient build-context staging of the AVS RA-TLS trust anchor.
# `make docker-build` copies $(SECRETS_DIR)/avs-tls.crt here so the Dockerfile
# can COPY it into /app/avs-ca.crt; the file is removed when the build exits.
/avs-ca.crt

SPRINT*